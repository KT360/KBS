import useState from 'react'

//Main window that manages each page, as a list and displays them 
export default function Window()
{

    const [curr_page,setPage] = useState(null); // set to home page

    return(<div>
        
    </div>);
}
